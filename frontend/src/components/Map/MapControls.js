import React from 'react';

const MapControls = () => {
  return (
    <div className="map-controls">
      <button>Map</button>
      <button>Satellite</button>
    </div>
  );
};

export default MapControls;
